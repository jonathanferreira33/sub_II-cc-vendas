version: "3.8"
services:
  h2:
    image: oscarfonts/h2
    container_name: h2-db
    ports:
      - "9092:9092"
      - "8082:8082"
    volumes:
      - h2-data:/opt/h2-data
    environment:
      H2_OPTIONS: -ifNotExists

volumes:
  h2-data:
