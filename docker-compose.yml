version: "3.8"
services:
  app-subiifiap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: subiifiap-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    stdin_open: true
    tty: true
    restart: unless-stopped
